<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= post.title %> - Simple Blog</title>
  <link rel="stylesheet" href="/styles.css">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body class="bg-light min-vh-100 d-flex flex-column">
  <header class="bg-primary text-white py-3 mb-4">
    <div class="container d-flex flex-wrap align-items-center justify-content-between">
      <h1 class="h3 m-0"><a href="/" class="text-white text-decoration-none">Simple Blog</a></h1>
      <nav>
        <a href="/new" class="btn btn-outline-light ms-2">New Post</a>
      </nav>
    </div>
  </header>
  <main class="container my-4 flex-grow-1">
    <article class="post-full card shadow-sm p-4 mb-4">
      <h2 class="mb-3"><%= post.title %></h2>
      <p><%= post.content %></p>
      <small class="text-muted"><%= post.date.toLocaleString('en-US') %></small>
      <div class="mt-3 d-flex gap-2">
        <a href="/post/<%= id %>/edit" class="btn btn-outline-primary btn-sm">Edit</a>
        <form action="/post/<%= id %>/delete" method="POST" style="display:inline;">
          <button type="submit" class="btn btn-outline-danger btn-sm">Delete</button>
        </form>
      </div>
    </article>
    <a href="/" class="btn btn-link">&larr; Back to home</a>
  </main>
  <footer class="bg-dark text-white text-center py-3 mt-5 mt-auto">
    &copy; <%= new Date().getFullYear() %> Simple Blog. All rights reserved.
  </footer>
</body>
</html>
