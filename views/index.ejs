<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Blog Simple</title>
  <link rel="stylesheet" href="/styles.css">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body class="bg-light min-vh-100 d-flex flex-column">
  <header class="bg-primary text-white py-3 mb-4">
    <div class="container d-flex flex-wrap align-items-center justify-content-between">
      <h1 class="h3 m-0"><a href="/" class="text-white text-decoration-none">Simple Blog</a></h1>
      <nav>
        <a href="/new" class="btn btn-outline-light ms-2">New Post</a>
      </nav>
    </div>
  </header>
  <main class="container my-4 flex-grow-1">
    <% if (posts.length === 0) { %>
      <p>No posts yet.</p>
    <% } %>
    <div class="row g-4">
    <% posts.forEach((post, idx) => { %>
      <div class="col-12 col-md-6 col-lg-4">
        <article class="post-preview card h-100 shadow-sm">
          <div class="card-body d-flex flex-column">
            <h2 class="card-title h5"><a href="/post/<%= idx %>" class="stretched-link text-decoration-none text-dark"><%= post.title %></a></h2>
            <p class="card-text flex-grow-1"><%= post.content.substring(0, 120) %>...</p>
            <small class="text-muted"><%= post.date.toLocaleString('en-US') %></small>
            <div class="mt-2 d-flex gap-2">
              <a href="/post/<%= idx %>/edit" class="btn btn-sm btn-outline-primary">Edit</a>
              <form action="/post/<%= idx %>/delete" method="POST" style="display:inline;">
                <button type="submit" class="btn btn-sm btn-outline-danger">Delete</button>
              </form>
            </div>
          </div>
        </article>
      </div>
    <% }) %>
    </div>
  </main>
  <footer class="bg-dark text-white text-center py-3 mt-5 mt-auto">
    &copy; <%= new Date().getFullYear() %> Simple Blog. All rights reserved.
  </footer>
</body>
</html>
